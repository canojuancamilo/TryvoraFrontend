export const environment = {
  production: true,
  apiUrl: 'https://api.tudominio.com/api',
  tokenKey: 'auth_token',
  refreshTokenKey: 'refresh_token',
  useSessionStorage: true, // Más seguro
  cacheEnabled: true
};